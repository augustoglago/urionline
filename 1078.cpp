#include <bits/stdc++.h>

int main(){
    int N;
    scanf("%d", &N);
    for (int count = 1; count < 11; count++){
        int S = count * N;
    printf("%d x %d = %d\n", count, N, S);
    }
    return 0;
}
